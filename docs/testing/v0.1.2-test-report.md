# WatermarkFlow v0.1.2 测试报告

## 范围

- 作者显示名称与 HTTPS 主页地址
- 关于窗口 Link 编译
- App `AuthorURL` 元数据
- 既有模板、渲染、持久化、迁移和剪贴板回归
- Release 构建、签名和本机覆盖安装

## 结果

- Swift 自动化测试：`10/10 PASS`，`0 FAIL`。
- 作者名称、HTTPS 作者主页和产品网站常量：通过。
- Release `AuthorURL=https://x.com/wlzh` 元数据：通过。
- 关于窗口辅助功能树：`X @wlzh` 为 link，目标值 `x.com/wlzh`。
- 产品网站辅助功能树：link 目标值 `869hr.uk`。
- 关于窗口实际排版：通过本机截图检查。
- 既有模板、拖动、持久化、迁移、渲染和 pasteboard 回归：通过。
- Release 版本 `0.1.2`、签名、本机覆盖安装和进程启动：通过。
