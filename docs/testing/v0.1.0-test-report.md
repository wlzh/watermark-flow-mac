# WatermarkFlow v0.1.0 测试报告

## 测试环境

- 日期：2026-09-06
- 操作系统：macOS 15.6 (24G84)
- 架构：Apple Silicon arm64
- Swift：6.1，Swift 5 language mode
- Xcode：未安装完整 Xcode，使用 Apple Command Line Tools；测试使用零依赖 Swift runner
- 签名：本机 ad-hoc

## 自动化范围

- 默认模板稳定性与字段检查
- 参数边界归一化
- 零位移单击稳定性、相对拖动和边缘限制
- 用户模板 JSON 持久化往返
- X、YouTube、文字、自定义 Logo 渲染
- 输出像素尺寸保持
- PNG 与 JPEG 编码
- 独立 pasteboard 图片写入读取
- Release 可执行文件真实 general pasteboard 自检
- general pasteboard 自检前后内容快照恢复
- App bundle 版本、结构和代码签名校验

## 执行结果

执行命令：

```bash
./scripts/test-all.sh
./scripts/install-local.sh
```

结果：

- Swift 自动化测试：`7/7 PASS`，`0 FAIL`。
- 三套内置模板渲染：通过。
- 文字和自定义 Logo 渲染：通过。
- PNG/JPEG 编码：通过。
- 独立 pasteboard 往返：通过。
- general pasteboard `960 × 540` 往返及原内容恢复：通过。
- Release App 构建、Info.plist、作者、网站和 ad-hoc 签名：通过。
- 安装位置：`/Applications/WatermarkFlow.app`。
- 安装后进程启动检查：通过。
- 已安装二进制自检：通过。

## 人工验收

- 应用启动后显示菜单栏图标和编辑窗口：通过。
- 内置模板可选择并显示正确品牌与文字：通过。
- `2000 × 1200` 剪贴板测试图载入并保持像素尺寸：通过。
- X Logo 与 `@wlzh` 组合水印预览：通过。
- 预览拖动整组水印改变位置：通过。
- 单击水印不改变位置、连续拖动按起始点相对移动：通过回归测试。
- “生成并复制”替换系统剪贴板图片：通过。
- 关闭窗口后应用仍驻留菜单栏：通过。
- 关于窗口改为独立固定布局，作者和网站分行展示：通过构建与元数据检查。

## 已知限制

- 自动化测试可以验证系统剪贴板真实数据往返，但聊天软件中的最终粘贴视觉仍属于应用间人工验收。
- 无 Developer ID，未测试 Gatekeeper 外部分发和 notarization。
- Carbon 全局快捷键注册代码已构建并随已安装 App 运行；是否与用户后续安装的软件产生冲突取决于本机实时状态。
