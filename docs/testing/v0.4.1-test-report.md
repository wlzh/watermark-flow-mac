# WatermarkFlow v0.4.1 测试报告

## 范围

- 版本：`0.4.1 (12)`
- 日期：2026-09-08
- 平台：macOS，Apple Silicon 主机

## 自动化矩阵

| 项目 | 预期 | 结果 |
| --- | --- | --- |
| Swift 核心测试 | 28 个用例全部通过 | 通过 |
| 鼠标滚轮分类 | 上下方向正确，精细和横向滚动透传 | 通过 |
| 滚轮缩放路由 | 使用既有相邻缩放档位 | 通过 |
| 缩放边界 | 25%–1000%，空画布无副作用 | 通过 |
| 图片拖动平移 | 水印命中优先，非水印区平移，坐标不变 | 通过 |
| 既有功能回归 | 模板、拖动、满屏、自适应、剪贴板和输出 | 通过 |
| Universal 架构 | `arm64` 与 `x86_64` | 通过 |
| 签名与 MIT 许可 | 签名有效，包内许可一致 | 通过 |
| 本机安装与界面 | 版本、控件、提示和运行状态 | 通过 |
| Release 资产 | ZIP、SHA-256、远端下载和解压 | 通过 |

## 性能

| 场景 | 调试构建实测 |
| --- | --- |
| 4000 × 3000 预览 | 约 24.3 ms，输出 1200 × 900 |
| 4000 × 3000 单水印输出 | 约 31.5 ms，输出 4000 × 3000 |
| 4000 × 3000 满屏 10 级输出 | 约 101.2 ms |
| 4000 × 3000 满屏 10 级自动对比输出 | 约 92.0 ms |

性能数据仅用于量级判断，不作为跨设备固定基准。画布平移直接更新滚动视口，不触发图片重新渲染或模板保存。

## 数据保护

- 模板 schema 保持 4，本版本不执行模板迁移。
- 安装前备份实际用户模板到 `templates-before-v0.4.1.json`。
- 最终候选安装、启动、300% 图片平移和恢复 100% 后，实际模板文件与备份 SHA-256 均为 `cf5328a87db0ee2d450ea258b3b84c8907ab3cd4ae91ea127106ce164348fd0e`。

## 本机与界面复验

- 安装路径：`/Applications/WatermarkFlow.app`；已安装版本：`0.4.1 (12)`。
- 实际主窗口载入 `1712 × 1290 px` 图片，显示滚轮缩放、图片拖动和水印定位提示，无新增重叠或截断。
- 通过真实鼠标拖动自动化将 300% 画布从水平/垂直滚动位置 `0/0` 移至约 `0.1548/0.2033`，画面切换到新的中部区域，缩放比例保持 300%。
- 平移后模板文件逐字节不变，证明图片和水印整体移动且水印模板坐标未修改；随后 `⌘0` 正常恢复 100%。
- Computer Use 的滚动操作被系统识别为精细滚动，按设计用于平移而不触发鼠标滚轮分支；标准滚轮路径由事件分类、路由和边界自检覆盖，未伪报为实体滚轮人工测试。
- 直接拖动水印会修改真实模板，未在用户配置上进行破坏性复验；实际水印边界、命中优先级和位置更新由核心测试及隔离应用自检覆盖。

## 发布包

- 文件：`WatermarkFlow-v0.4.1-macos-universal.zip`
- 大小：843705 字节
- SHA-256：`791e730eac18ec97b28f2a49d2b48961c18df954f9890f9214c431e724382742`
- `shasum -a 256 -c`：通过
- `unzip -t`：无错误
- GitHub Release：https://github.com/wlzh/watermark-flow-mac/releases/tag/v0.4.1
- Release 状态：公开 prerelease，非草稿
- 远端重新下载 SHA-256：与本地一致
- 远端重新下载 `unzip -t`：无错误
- 发布标签提交：`5af2a5d74037d7cdcb68bd79ad427f7535dfdc30`
- 发布时远端 `main`：`5af2a5d74037d7cdcb68bd79ad427f7535dfdc30`

## 发布后文档一致性复核

- 复核日期：2026-09-08。
- `VERSION`、`BUILD_NUMBER`、`AppVersion.swift`、Info.plist、安装说明和本报告统一为 `0.4.1 (12)`。
- 修正 v0.4.1 PRD 的逐级缩放列表：实际档位为 25%、50%、75%、100%、125%、150%、200%、300%、400%、500%、750%、1000%。
- 12 个 Git 标签与 12 个 Changelog 版本完全对应；每个版本均有 PRD、设计、开发、计划、测试报告和 Release Notes。
- 77 个 Markdown 文件的本地链接检查为 0 个断链。
- GitHub 在线复核确认 `v0.2.1`、`v0.3.1`、`v0.3.2`、`v0.4.0`、`v0.4.1` 保留 ZIP 与 SHA256 资产；其余 7 个版本仅保留 Git 标签和仓库文档。
- 新增 `scripts/check-docs.sh` 并纳入 `scripts/test-all.sh`；离线输出为 `DOCS_CHECK=PASS`、`DOCS_RELEASE_COUNT=12`、`DOCS_MARKDOWN_COUNT=77`。

## 已知分发限制

应用使用 ad-hoc 签名且未做 Apple notarization。首次在其他 Mac 上启动可能需要通过 Finder 的“打开”操作确认。
