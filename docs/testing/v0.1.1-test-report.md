# WatermarkFlow v0.1.1 测试报告

## 范围

- 完整模板库新版 JSON 往返
- v0.1.0 数组格式迁移
- 内置模板覆盖与出厂模板合并
- 最后选择状态恢复
- 所有颜色、Logo、位置、大小、旋转和透明度字段保持
- 既有渲染、拖动、编码和剪贴板回归
- 独立系统 pasteboard 名称往返，不修改用户通用剪贴板
- Release 构建、App 元数据、签名和本机安装

## 结果

执行命令：

```bash
./scripts/test-all.sh
./scripts/install-local.sh
/Applications/WatermarkFlow.app/Contents/MacOS/WatermarkFlow --self-test
```

结果：

- Swift 自动化测试：`9/9 PASS`，`0 FAIL`。
- 完整模板字段和自定义 Logo 数据往返：通过。
- 内置模板覆盖与最后选择恢复：通过。
- `v0.1.0` 数组格式迁移：通过。
- 250 ms 自动保存后重新创建编辑器实例：通过。
- 自动保存恢复文字 `@automatic`、位置 `(0.31, 0.42)` 和背景色：通过。
- 三套模板 `960 × 540` 渲染：通过。
- 独立系统 pasteboard 往返：通过，不修改 general pasteboard。
- Release 版本 `0.1.1`、作者、网站和签名：通过。
- 安装位置 `/Applications/WatermarkFlow.app`，启动进程检查：通过。
- 已安装二进制 `--self-test`：通过，正常退出。

## 已知边界

- 原图和生成图继续不持久化，符合当前会话内无损编辑约束。
- 当前使用 ad-hoc 签名，不覆盖外部分发与 notarization。
