# WatermarkFlow v0.4.5 测试报告

## 范围

- 版本：`0.4.5 (16)`
- 日期：2026-09-19
- 平台：macOS，Apple Silicon 主机
- 模板 schema：`4`

## 复现证据

- 已安装 `0.4.4 (15)` 正常运行，剪贴板含微信临时目录中的单个 `public.png` 文件 URL。
- 文件存在、可读，大小 415,755 字节，尺寸 1320×2868；AppKit 可从文件 URL 解码。
- WatermarkFlow 主窗口显示“检测到新的剪贴板图片”，证明来源识别成功。
- 默认快捷键显示为 `⌥⌘W`；Apple Human Interface Guidelines 将该组合定义为“关闭当前应用的全部窗口”，存在前台应用菜单冲突。
- 点击“恢复默认”时 Carbon 注册返回成功，但自动化按键不能替代真实系统全局热键事件，因此最终验收单独使用物理键盘。

## 自动化矩阵

| 项目 | 预期 | 结果 |
| --- | --- | --- |
| Swift 核心测试 | 渲染、模板、文件剪贴板和 HTML 回归 | 通过，33/33 |
| 默认快捷键 | 显示并保存 `⌃⌥⌘W` | 通过应用自检 |
| 旧默认迁移 | `⌥⌘W` 自动写回新默认 | 通过应用自检 |
| 自定义快捷键 | `⇧⌘K` 保持并跨实例恢复 | 通过应用自检 |
| 用户模板保护 | 安装前后模板 JSON 不变 | 待安装验证 |
| Universal、签名、许可 | 双架构、签名及 MIT 资源有效 | 通过，`x86_64 arm64` |
| Release 资产 | ZIP、SHA-256、远端下载及解压通过 | 待发布验证 |

## 自动化结果

- `./scripts/test-all.sh`：通过。
- `TEST_CASES_PASSED=33`，`TEST_CASES_FAILED=0`。
- `SELF_TEST_DEFAULT_HOTKEY_MIGRATION=PASS value=⌃⌥⌘W`。
- `SELF_TEST_CUSTOM_HOTKEY=PASS value=⇧⌘K`。
- `BUILD_APP=PASS`，版本 `0.4.5 (16)`，架构 `x86_64 arm64`。
- 文档、作者链接、MIT 许可、应用图标和版本化发布文档检查通过。

## 本机真实键盘

- 待安装后使用物理键盘按 `⌃⌥⌘W`，验证当前微信图片文件 URL 被替换为同尺寸带水印 PNG/TIFF。

## 发布包

- 文件：`WatermarkFlow-v0.4.5-macos-universal.zip`
- 大小：待打包回填。
- SHA-256：待打包回填。
- GitHub Release：https://github.com/wlzh/watermark-flow-mac/releases/tag/v0.4.5

## 已知分发限制

应用使用 ad-hoc 签名且未做 Apple notarization。首次在其他 Mac 上启动可能需要通过 Finder 的“打开”操作确认。
