# WatermarkFlow v0.2.2 测试报告

## 范围

- 版本：`0.2.2 (6)`
- 日期：2026-09-06
- 平台：macOS，Apple Silicon 主机

## 自动化结果

| 项目 | 预期 | 结果 |
| --- | --- | --- |
| Swift 核心测试 | 15 个用例全部通过 | 通过 |
| source-only 渲染 | 原图尺寸不变、预览正确限幅 | 通过 |
| 移除与恢复 | 隐藏后输出原图、恢复后重新显示 | 通过 |
| 模板替换 | 隐藏后选择模板自动恢复且不叠加 | 通过 |
| 大图缩放 | 25% 至 1000% 分档、百分比选择、100% 重置 | 通过 |
| 空画布菜单 | 导出、生成、移除和清空均禁用 | 通过 |
| 应用自检 | 所有 `SELF_TEST_*` 检查通过 | 通过 |
| Universal 架构 | `arm64` 与 `x86_64` | 通过 |
| 签名校验 | `codesign --verify --deep --strict` 通过 | 通过 |
| 本机安装启动 | `/Applications/WatermarkFlow.app` 正常运行 | 通过 |

## 性能

| 场景 | 结果 |
| --- | --- |
| 4000 × 3000 预览渲染 | 约 19.5 ms，输出 1200 × 900 |
| 4000 × 3000 全尺寸渲染 | 约 6.8 ms，输出 4000 × 3000 |
| 放大预览保护 | 7000 × 700 输入限制为 6000 × 600；小图不放大，通过 |

性能数值来自同一轮调试测试，仅用于量级判断，实际耗时随系统负载变化。

## 界面复验

- 空画布下“移除水印”、导出、清空和生成按钮均正确禁用。
- 有图画布显示缩放工具条、百分比状态、双向滚动条和动态“移除水印”入口。
- 主窗口暗色模式下标题、检查器、按钮和状态文字层级清晰，无高亮白色干扰。
- 应用菜单包含“文件”“编辑”“显示”；缩放与水印操作均有原生菜单入口。

## 发布包

- 文件：`WatermarkFlow-v0.2.2-macos-universal.zip`
- 大小：649607 字节
- SHA-256：`18c221c092e4ae8054b8327e24aa66ce5acfb980306a89aa1d794eed6c51f81a`
- `shasum -a 256 -c`：通过
- `unzip -t`：压缩数据无错误

## 已知分发限制

应用仍使用 ad-hoc 签名且未做 Apple notarization。首次在其他 Mac 上启动可能需要通过 Finder 的“打开”操作确认。
