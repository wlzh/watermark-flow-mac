# WatermarkFlow v0.2.3 开发记录

## 变更

- 新增标准 MIT `LICENSE`，版权人为 `wlzh`。
- `AppVersion` 增加许可证名称和公开链接。
- `AboutView` 增加许可证入口。
- `Info.plist` 增加 `License` 字段。
- `build-app.sh` 将许可证复制进 App Resources。
- `test-all.sh` 增加许可证元数据和内容一致性门禁。

## 版本同步

`VERSION`、`BUILD_NUMBER`、`AppVersion`、Info.plist、README、CHANGELOG、PRD、测试报告和 Release 文件名统一为 `0.2.3 (7)`。
