# Changelog

所有重要变更均记录在此文件，版本号遵循 Semantic Versioning。

## [0.2.1] - 2026-09-06

### Added

- 新增空画布菜单状态、自定义 Logo 限幅、竖图、小图和透明通道回归测试。
- 新增独立 `BUILD_NUMBER`，发布校验同步检查源码、App、README、PRD 和测试报告版本。

### Changed

- 主菜单在空画布时禁用导出、生成和清空，剪贴板载入增加 `⇧⌘V`。
- 自定义 Logo 导入时最长边限制为 1024 像素并保留透明通道，降低模板存储和自动保存开销。
- 测试报告更新为本版本实际界面、安装包和 Release 验证结果。

## [0.2.0] - 2026-09-06

### Added

- 菜单栏增加动态模板子菜单，可直接选择任意已保存模板完成剪贴板水印处理。
- 全局快捷键支持录制、更换、自动保存、冲突回退和恢复默认 `⌥⌘W`。
- 编辑画布增加右键清空和底部“清空画布”按钮，清除当前图片但保留模板设置。
- 深色与浅色模式分别采用完整语义色板，降低暗色界面的纯白眩光。
- 增加 GitHub Release 打包脚本、SHA-256 校验文件和安装说明。

### Changed

- 全局快捷键 `⌥⌘W` 保持使用默认模板，菜单子项用勾选标识当前默认模板。
- 交互预览最长边限制为 1200 像素；复制和导出仍从原图全分辨率渲染。
- 完善作者、网站、版本、分发限制和验收矩阵文档。

## [0.1.2] - 2026-09-06

### Changed

- 关于窗口中的作者 `X @wlzh` 改为可点击链接，目标为 `https://x.com/wlzh`。
- 作者主页和产品网站提升为统一应用元数据并加入构建校验。

## [0.1.1] - 2026-09-06

### Added

- 自动保存每个模板的文字、Logo、前景色、背景色、图标色、透明度、大小、位置和旋转角度。
- 自动保存最后选中的模板，重启应用和载入下一张图片时直接沿用。
- 内置模板支持持久化覆盖，同时保持不可删除和出厂默认回退能力。
- 模板存储增加 `schemaVersion`，兼容迁移 `v0.1.0` 用户模板数组。

### Changed

- 模板参数编辑采用短延迟自动保存，切换模板、设为默认和退出应用时强制落盘。
- 发布自检改用独立 pasteboard，避免覆盖或恢复微信等应用的私有剪贴板格式。

## [0.1.0] - 2026-09-06

### Added

- 原生 macOS 菜单栏应用和非破坏式单图编辑会话。
- 剪贴板、拖放、文件三种图片导入方式。
- 文字、X、YouTube、自定义 Logo 四类水印。
- 位置、前景色、背景色、图标色、透明度、大小和旋转设置。
- 三套 wlzh 默认水印模板。
- 用户模板持久化、默认模板选择和删除功能。
- PNG/JPEG 导出、PNG/TIFF 剪贴板输出。
- `⌥⌘W` 全局快速水印快捷键。
- 单元测试、真实剪贴板自检、构建和本地安装脚本。
- 拖动采用按下起点加相对位移，修复单击水印时跳位和绕鼠标移动的问题。
- 独立关于窗口统一展示产品、版本、作者、网站和隐私说明。
