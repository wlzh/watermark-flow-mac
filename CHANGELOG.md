# Changelog

所有重要变更均记录在此文件，版本号遵循 Semantic Versioning。

## [0.3.2] - 2026-09-07

### Added

- 增加“新建 Logo + 文字模板”入口，先创建独立用户模板再导入 Logo、输入文字和调整样式，不再要求先修改已有模板。
- 自定义 Logo 区显示“尚未选择”或已嵌入文件大小；Logo 数据存在但当前图标类型未启用时显示恢复入口。
- 增加真实 PNG + 文字模板的创建、源模板隔离、持久化、重启恢复、复制和快速渲染端到端测试。

### Changed

- “品牌”改称“图标类型”，“自定义 Logo”改称“自定义 Logo + 文字”，明确自定义 Logo 与文字可以组合使用。
- “另存模板”改称“复制当前模板”，对话框明确复制当前全部设置，避免与新建模板流程混淆。
- Logo 导入失败不再静默，编辑器会显示可操作的错误信息。

## [0.3.1] - 2026-09-07

### Fixed

- 单个水印与满屏水印不再共用同一组视觉参数；每个模板分别保存两种布局的文字色、背景色、图标色、透明度、大小和旋转。
- 切换布局时立即恢复该布局上次保存的参数；单个模式继续独立保留位置，满屏模式继续独立保留密度。

### Changed

- 模板存储 schema 升级为 3；升级 `v0.3.0` 时将原有共享参数复制到两套配置，保持已有模板外观不变。
- 编辑器明确显示当前正在编辑“单个样式”或“满屏样式”，降低误改另一种布局配置的风险。
- 增加双配置隔离、活动配置渲染、自动保存重启和两级旧数据迁移测试。

## [0.3.0] - 2026-09-07

### Added

- 模板新增“单个 / 满屏”布局模式，一键把当前文字、Logo 和组合水印错列平铺到整张图片。
- 满屏模式新增 1–10 级密度调节，继续复用颜色、透明度、大小和旋转参数。
- 增加满屏覆盖量、密度差异、4K 平铺性能、旧模板迁移和持久化测试。

### Changed

- 模板存储 schema 升级为 2；旧模板缺少布局字段时自动使用单水印和 5 级密度。
- 满屏模式不响应水印拖动，切回单个后继续使用原先保存的位置。

## [0.2.3] - 2026-09-06

### Added

- 项目采用 MIT License，仓库根目录增加标准 `LICENSE` 文件。
- About 窗口增加 MIT License 链接，应用元数据增加许可证字段。
- macOS 安装包内置 `Contents/Resources/LICENSE.txt`，构建门禁校验仓库与包内许可文本一致。

## [0.2.2] - 2026-09-06

### Added

- 当前编辑会话支持单独移除或重新添加水印，原图和模板设置保持不变。
- 画布右键菜单、检查器状态区、底部工具栏和应用菜单均提供动态“移除/添加水印”操作。
- 大图画布增加 25% 至 1000% 分档缩放、百分比选择、滚动查看及 `⌘+`、`⌘-`、`⌘0` 菜单快捷键。
- 增加无水印原图渲染、移除恢复、模板替换和菜单状态回归测试。

### Changed

- 移除水印后复制或导出会输出无水印原图；选择其他模板会直接替换并恢复水印，不产生叠加。
- 新载入图片和清空后再次载入时默认显示所选模板水印，水印可见状态不跨会话持久化。
- 缩放时按需提高预览渲染分辨率，最高限制为 6000 像素；缩放不改变原图尺寸和水印归一化位置。

## [0.2.1] - 2026-09-06

### Added

- 新增空画布菜单状态、自定义 Logo 限幅、竖图、小图和透明通道回归测试。
- 新增独立 `BUILD_NUMBER`，发布校验同步检查源码、App、README、PRD 和测试报告版本。

### Changed

- 主菜单在空画布时禁用导出、生成和清空，剪贴板载入增加 `⇧⌘V`。
- 自定义 Logo 导入时最长边限制为 1024 像素并保留透明通道，降低模板存储和自动保存开销。
- 测试报告更新为本版本实际界面、安装包和 Release 验证结果。

## [0.2.0] - 2026-09-06

### Added

- 菜单栏增加动态模板子菜单，可直接选择任意已保存模板完成剪贴板水印处理。
- 全局快捷键支持录制、更换、自动保存、冲突回退和恢复默认 `⌥⌘W`。
- 编辑画布增加右键清空和底部“清空画布”按钮，清除当前图片但保留模板设置。
- 深色与浅色模式分别采用完整语义色板，降低暗色界面的纯白眩光。
- 增加 GitHub Release 打包脚本、SHA-256 校验文件和安装说明。

### Changed

- 全局快捷键 `⌥⌘W` 保持使用默认模板，菜单子项用勾选标识当前默认模板。
- 交互预览最长边限制为 1200 像素；复制和导出仍从原图全分辨率渲染。
- 完善作者、网站、版本、分发限制和验收矩阵文档。

## [0.1.2] - 2026-09-06

### Changed

- 关于窗口中的作者 `X @wlzh` 改为可点击链接，目标为 `https://x.com/wlzh`。
- 作者主页和产品网站提升为统一应用元数据并加入构建校验。

## [0.1.1] - 2026-09-06

### Added

- 自动保存每个模板的文字、Logo、前景色、背景色、图标色、透明度、大小、位置和旋转角度。
- 自动保存最后选中的模板，重启应用和载入下一张图片时直接沿用。
- 内置模板支持持久化覆盖，同时保持不可删除和出厂默认回退能力。
- 模板存储增加 `schemaVersion`，兼容迁移 `v0.1.0` 用户模板数组。

### Changed

- 模板参数编辑采用短延迟自动保存，切换模板、设为默认和退出应用时强制落盘。
- 发布自检改用独立 pasteboard，避免覆盖或恢复微信等应用的私有剪贴板格式。

## [0.1.0] - 2026-09-06

### Added

- 原生 macOS 菜单栏应用和非破坏式单图编辑会话。
- 剪贴板、拖放、文件三种图片导入方式。
- 文字、X、YouTube、自定义 Logo 四类水印。
- 位置、前景色、背景色、图标色、透明度、大小和旋转设置。
- 三套 wlzh 默认水印模板。
- 用户模板持久化、默认模板选择和删除功能。
- PNG/JPEG 导出、PNG/TIFF 剪贴板输出。
- `⌥⌘W` 全局快速水印快捷键。
- 单元测试、真实剪贴板自检、构建和本地安装脚本。
- 拖动采用按下起点加相对位移，修复单击水印时跳位和绕鼠标移动的问题。
- 独立关于窗口统一展示产品、版本、作者、网站和隐私说明。
